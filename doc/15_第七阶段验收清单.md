# 第七阶段验收清单

## 阶段目标

完成全局音频、设置、发布配置和最终回归，将六关主线整理为可统一人工验收的 1.0 基线。

## 自动验收

- [x] 菜单与第一至第六关共7条独立BGM均为实际PCM音频并可循环播放
- [x] 攻击、受击、拾取和胜利4类音效均为实际PCM音频并接入玩法事件
- [x] Music、SFX与Master混音总线可即时调节
- [x] 三路音量和减少界面动画设置可写入并恢复存档
- [x] 暂停菜单控件、布局与焦点操作正常
- [x] 减少界面动画开启后不执行章节标题淡出
- [x] 第一至第六关入口均可加载，主菜单显示完整六关版本
- [x] 第一阶段攻击按下不触发、松开触发、长按不连续攻击回归通过
- [x] 第一至第六阶段角色逐帧动画、攻击特效、实体碰撞、路线顶边线回归通过
- [x] 六关敌人、陷阱、Boss阶段与最终通关状态回归通过

专项命令：

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . res://tests/run_phase_07.tscn
```

结果：17项检查通过。第一至第六阶段测试同步全部通过。

## 实际运行验收

- [x] 主菜单完整版本标题、背景和按钮布局正常
- [x] 暂停面板三路音量与减少界面动画控件无溢出
- [x] macOS 应用包导出成功
- [x] 导出包为 x86_64 + arm64 universal
- [x] 导出后二进制可加载PCK并启动主菜单，无脚本或资源加载错误

截图：

- `artifacts/phase_07/main_menu_actual.png`
- `artifacts/phase_07/audio_settings_actual.png`

构建产物：`builds/Havoc.app`，约202 MB。该目录已忽略，不纳入Git仓库。

## 最终基线审计

- [x] 路线图第一至第七阶段全部完成
- [x] 数值继续由 `.tres` 资源维护
- [x] 新增素材均为可直接运行的实际资源，无占位内容
- [x] 六关保持独立场景构图并与实际行走路线对齐
- [x] 所有阶段实现、测试、截图和发布说明可从仓库追溯

## 剩余风险

- macOS 包未签名、公证，只适合本机与开发验收。
- Windows/Web 未做构建验证，不属于本次完成范围。
- 部分测试进程强制退出时有 Godot 资源清理警告，未出现功能断言、脚本解析或构建失败。

结论：第七阶段自验收通过，可进入完整六关统一人工验收。
